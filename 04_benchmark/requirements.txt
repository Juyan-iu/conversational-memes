# ════════════════════════════════════════════════════════════════
#  benchmark_pipeline.py dependency
#
#
#  1) PyTorch (CUDA 12.1):
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
#  2) simple-lama-inpainting (Pillow<10 ):
#     pip install simple-lama-inpainting
#
#  3) pip install -r requirements.txt
#
#  * Pillow version = 9.x
# ════════════════════════════════════════════════════════════════

# ── Core ────────────────────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
numpy>=1.24.0
# simple-lama-inpainting 
Pillow>=9.5.0,<10.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# ── Inpainting (LaMa GPU) ────────────────────────────────────────
simple-lama-inpainting>=0.1.2

# ── CLIP (ViT-L-14) ─────────────────────────────────────────────
open_clip_torch>=2.24.0

# ── OCR ─────────────────────────────────────────────────────────
easyocr>=1.7.1

#keyword
yake>=0.4.8

# ── OpenCV (inpainting fallback) ─────────────────────────────────
opencv-python>=4.9.0
